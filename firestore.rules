rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for all banking collections to enable full real-time database CRUD sync
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
